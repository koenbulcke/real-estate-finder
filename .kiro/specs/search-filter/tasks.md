# Search & Filter Feature — Tasks

- [ ] 1. Create `src/components/SearchFilterBar.jsx` with text search + min/max price inputs + Clear button
- [ ] 2. Update `PropertiesPage.jsx` to add filter state and derived `filteredProperties`
- [ ] 3. Import and render `<SearchFilterBar>` in `PropertiesPage.jsx`
- [ ] 4. Pass `filteredProperties` to `<PropertyList>` instead of `properties`
- [ ] 5. Add "No properties found" empty state when `filteredProperties.length === 0`
